export default "به</th>\n          <th>کمپین</th>\n          <th>منشا</th>\n          <th>آخرین وضعیت</th>\n          <th>دلیل تماس بعدی</th>\n        </tr></thead>\n        <tbody>\n          ${rows.map(r=>`<tr>\n            <td>${safe(r.owner||'—')}</td>\n            <td>${safe(r.leadNumber||'—')}</td>\n            <td>${safe(r.customerName||'—')}</td>\n            <td>${safe(r.leadType||'—')}</td>\n            <td>${fmt(r.createdDate)}</td>\n            <td>${fa(r.ageDays)} روز</td>\n            <td><b>${fa(r.remainingDays)} روز</b></td>\n            <td>${safe(r.customerRank||'—')}</td>\n            <td>${safe(r.campaign||'—')}</td>\n            <td>${safe(r.source||'—')}</td>\n            <td>${safe(r.lastStatus||'—')}</td>\n            <td>${safe(r.nextCallReason||'بدون تسک')}</td>\n          </tr>`).join('')}\n        </tbody>\n       </table>\n      </div>`;\n\n    makeTableSortable(box);\n    box.scrollIntoView({behavior:'smooth',block:'start'});\n  })\n  .getOpenNearDeadlineDetails(\n    leadType,\n    owner,\n    filterState.open\n  );\n}\n\nfunction loadOpenDetail(field,value){\n const box=$('openDetail');box.style.display='block';$('openDetailSub').textContent='در حال دریافت...';$('openDetailBody').innerHTML='<div class=\"empty\">در حال خواندن جزئیات...</div>';\n google.script.run.withFailureHandler(e=>{$('openDetailBody').innerHTML='<div class=\"data-note\">'+safe(e.message||e)+'</div>'}).withSuccessHandler(rows=>{\n  $('openDetailSub').textContent=fa(rows.length)+' رکورد';\n  $('openDetailBody').innerHTML=`<div class=\"table-wrap activity-detail-wrap\"><table class=\"table\"><thead><tr><th>مشاور</th><th>شماره سرنخ</th><th>نام مشتری</th><th>تاریخ ثبت</th><th>سن</th><th>نوع</th><th>رتبه</th><th>کمپین</th><th>منشا</th><th>نرم‌افزار منشا</th><th>آخرین وضعیت</th><th>دلیل تماس بعدی</th><th>واحد تجاری</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${safe(r.owner||'—')}</td><td>${safe(r.leadNumber||'—')}</td><td>${safe(r.customerName||'—')}</td><td>${fmt(r.createdDate)}</td><td>${fa(r.ageDays)}</td><td>${safe(r.leadType||'—')}</td><td>${safe(r.customerRank||'—')}</td><td>${safe(r.campaign||'—')}</td><td>${safe(r.source||'—')}</td><td>${safe(r.sourceSoftware||'—')}</td><td>${safe(r.lastStatus||'—')}</td><td>${safe(r.nextCallReason||'بدون تسک')}</td><td>${safe(r.businessUnit||'—')}</td></tr>`).join('')}</tbody></table></div>`;\n  makeTableSortable(box);box.scrollIntoView({behavior:'smooth',block:'start'});\n }).getOpenLeadDetails(field,value);\n}\n\nfunction activityCards(k,period){\n const title=period==='daily'?'فعالیت کل':period==='weekly'?'فعالیت کل هفته':'فعالیت کل ماه';\n return `<div class=\"activity-kpis weekly-kpis\">\n  <div class=\"kpi\"><div class=\"kpi-label\">${title}</div><div class=\"kpi-value\">${fa(k.total)}</div><div class=\"kpi-sub\">لید صحبت‌شده + فرصت OPP + تماس + تیکت</div></div>\n  <div class=\"kpi\"><div class=\"kpi-label\">لید بسته‌شده</div><div class=\"kpi-value\">${fa(k.closed)}</div><div class=\"kpi-sub\">میانگین زمان بستن ${k.closeAvg==null?'—':Number(k.closeAvg).toFixed(1)+' روز'}</div></div>\n  <div class=\"kpi\"><div class=\"kpi-label\">لید صحبت‌شده</div><div class=\"kpi-value\">${fa(k.talked)}</div><div class=\"kpi-sub\">مبنای فعالیت</div></div>\n  <div class=\"kpi\"><div class=\"kpi-label\">فرصت از LEAD</div><div class=\"kpi-value\">${fa(k.oppLead)}</div><div class=\"kpi-sub\">صرفاً آماری</div></div>\n  <div class=\"kpi\"><div class=\"kpi-label\">فرصت از OPP</div><div class=\"kpi-value\">${fa(k.opp)}</div><div class=\"kpi-sub\">داخل فعالیت کل</div></div>\n  <div class=\"kpi\"><div class=\"kpi-label\">کل تماس</div><div class=\"kpi-value\">${fa(k.calls)}</div><div class=\"kpi-sub\">${fa(k.uniqueCalls)} لید یکتا / ${fa(k.repeatCalls)} تماس اضافه</div></div>\n  <div class=\"kpi\"><div class=\"kpi-label\">تماس ورودی T8</div><div class=\"kpi-value\">${fa(k.t8)}</div><div class=\"kpi-sub\">زیرمجموعه تماس‌ها</div></div>\n  <div class=\"kpi\"><div class=\"kpi-label\">تیکت</div><div class=\"kpi-value\">${fa(k.tickets)}</div><div class=\"kpi-sub\">رسیدگی‌شده</div></div>\n  ${period==='daily'?'':`\n   <div class=\"kpi accent-kpi\"><div class=\"kpi-label\">سرانه تماس روزانه واحد</div><div class=\"kpi-value\">${Number(k.callAvg||0).toFixed(2)}</div></div>\n   <div class=\"kpi accent-kpi\"><div class=\"kpi-label\">سرانه لید بسته در روز</div><div class=\"kpi-value\">${Number(k.closedAvg||0).toFixed(2)}</div></div>\n   <div class=\"kpi accent-kpi\"><div class=\"kpi-label\">سرانه لید صحبت‌شده در روز</div><div class=\"kpi-value\">${Number(k.talkedAvg||0).toFixed(2)}</div></div>`}\n </div>`\n}\n\nfunction renderPeriod(period,pageId,title){\n const s=state.data[period]||{},k=s.kpis||{},d=s.dimensions||{};\n $(pageId).innerHTML=`\n  ${reportBadge(period==='daily'?'تاریخ گزارش':'بازه گزارش',rangeText(s))}\n  <div class=\"topbar\"><div class=\"title\"><h2>${title}</h2><p>نمای مدیریتی یکپارچه فعالیت، روند روزانه و سرانه عملکرد مشاوران</p></div><div class=\"pill\">۴ منبع ${period==='daily'?'روزانه':period==='weekly'?'هفتگی':'ماهانه'}</div></div>\n  ${periodFiltersHtml(period)}\n  ${activityCards(k,period)}\n  ${period==='daily'?'':`<div class=\"grid2 weekly-chart-grid\"><div class=\"panel\"><div class=\"panel-head\"><div><h3>روند روزانه کل واحد</h3><p>لید صحبت‌شده، فرصت OPP، تماس، T8، تیکت و فعالیت کل</p></div></div><div class=\"line-chart\">${chart(s.trend)}</div></div><div class=\"panel\"><div class=\"panel-head\"><div><h3>روند روزانه مشاور</h3><p>برای مشاهده روند فردی، مشاور را انتخاب کنید.</p></div><div class=\"chart-select\"><select id=\"${period}TrendAdvisor\"><option value=\"__ALL__\" selected>کل تیم</option>${((filterOptions.team||{}).advisor||[]).map(name=>`<option value=\"${safe(name)}\">${safe(name)}</option>`).join('')}</select></div></div><div id=\"${period}AdvisorChart\" class=\"line-chart\">${chart(s.teamTrend||[])}</div></div></div>`}\n  <div class=\"panel activity-main-panel\"><div class=\"panel-head\"><div><h3>${period==='daily'?'فعالیت به تفکیک مشاور':'فعالیت و سرانه به تفکیک مشاور'}</h3><p>مقایسه حجم فعالیت و تشخیص افراد کم‌فعال/پرفعال</p></div><span class=\"badge\">${fa((s.advisors||[]).length)} نفر</span></div>\n   <div class=\"table-wrap activity-table-wrap\"><table class=\"table\"><thead><tr><th>مشاور</th><th>تیم لید</th><th>تیم</th><th>لید بسته</th><th>لید صحبت‌شده ⓘ</th><th>فرصت OPP</th><th>تماس</th><th>T8</th><th>تیکت</th><th>فعالیت کل</th>${period==='daily'?'':'<th>سرانه تماس</th><th>سرانه لید بسته</th><th>سرانه لید صحبت‌شده</th><th>سرانه OPP</th><th>سرانه T8</th><th>سرانه تیکت</th><th>سرانه فعالیت</th>'}</tr></thead><tbody>${(s.advisors||[]).map(r=>`<tr><td><b>${safe(r.name)}</b></td><td>${safe(r.teamLead||'—')}</td><td>${safe(r.team||'—')}</td><td><";
