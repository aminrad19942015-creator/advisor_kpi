export default "span class=\"activity-num\" data-p=\"${period}\" data-t=\"lead\" data-u=\"${safe(r.name)}\">${fa(r.leads)}</span></td><td><span class=\"activity-num\" data-p=\"${period}\" data-t=\"talked\" data-u=\"${safe(r.name)}\">${fa(r.talked)}</span></td><td><span class=\"activity-num\" data-p=\"${period}\" data-t=\"opp\" data-u=\"${safe(r.name)}\">${fa(r.opps)}</span></td><td><span class=\"activity-num\" data-p=\"${period}\" data-t=\"call\" data-u=\"${safe(r.name)}\">${fa(r.calls)}</span></td><td><span class=\"activity-num\" data-p=\"${period}\" data-t=\"t8\" data-u=\"${safe(r.name)}\">${fa(r.t8)}</span></td><td><span class=\"activity-num\" data-p=\"${period}\" data-t=\"ticket\" data-u=\"${safe(r.name)}\">${fa(r.tickets)}</span></td><td><b>${fa(r.total)}</b></td>${period==='daily'?'':`<td>${Number(r.callAvg||0).toFixed(2)}</td><td>${Number(r.leadAvg||0).toFixed(2)}</td><td>${Number(r.talkedAvg||0).toFixed(2)}</td><td>${Number(r.oppAvg||0).toFixed(2)}</td><td>${Number(r.t8Avg||0).toFixed(2)}</td><td>${Number(r.ticketAvg||0).toFixed(2)}</td><td><b>${Number(r.activityAvg||0).toFixed(2)}</b></td>`}</tr>`).join('')}</tbody></table></div>\n  </div>\n  <div class=\"panel activity-detail-panel\" id=\"${period}Detail\" style=\"display:none\"><div class=\"panel-head\"><div><h3>جزئیات فعالیت</h3><p id=\"${period}DetailSub\">—</p></div><button class=\"clear\" onclick=\"$('${period}Detail').style.display='none'\">بستن</button></div><div id=\"${period}DetailBody\"></div></div>\n  <div class=\"grid2\" id=\"${period}Dims1\">\n   <div class=\"panel\" data-source=\"lead\" data-field=\"lastStatus\"><div class=\"panel-head\"><div><h3>آخرین وضعیت لیدها</h3><p>توزیع وضعیت نهایی</p></div></div>${bars(d.leadState)}</div>\n   <div class=\"panel\" data-source=\"lead\" data-field=\"customerRank\"><div class=\"panel-head\"><div><h3>رتبه مشتری لیدها</h3><p>توزیع رتبه مشتری</p></div></div>${bars(d.rank)}</div>\n   <div class=\"panel\" data-source=\"lead\" data-field=\"source\"><div class=\"panel-head\"><div><h3>لید از منشا</h3><p>تعداد به تفکیک منشا</p></div></div>${bars(d.leadSource)}</div>\n   <div class=\"panel\" data-source=\"lead\" data-field=\"campaign\"><div class=\"panel-head\"><div><h3>کمپین لیدها</h3><p>کمپین‌های پرتکرار</p></div></div>${bars(d.campaign)}</div>\n  </div>\n  <div class=\"grid2\" id=\"${period}Dims2\">\n   <div class=\"panel\" data-source=\"opp\" data-field=\"registrationType\"><div class=\"panel-head\"><div><h3>فرصت‌ها: LEAD در برابر OPP</h3><p>نوع ایجاد فرصت</p></div></div>${bars(d.oppKind)}</div>\n   <div class=\"panel\" data-source=\"opp\" data-field=\"status\"><div class=\"panel-head\"><div><h3>وضعیت فرصت‌ها</h3><p>Open / Won / Lost و سایر وضعیت‌ها</p></div></div>${bars(d.oppStatus)}</div>\n   <div class=\"panel\" data-source=\"ticket\" data-field=\"statusReason\"><div class=\"panel-head\"><div><h3>علت وضعیت تیکت</h3><p>خروجی تیکت‌ها</p></div></div>${bars(d.ticketState)}</div>\n   <div class=\"panel\" data-source=\"ticket\" data-field=\"contactTopic\"><div class=\"panel-head\"><div><h3>موضوع تیکت</h3><p>موضوعات پرتکرار</p></div></div>${bars(d.ticketSubject)}</div>\n   <div class=\"panel\" data-source=\"call\" data-field=\"subject\"><div class=\"panel-head\"><div><h3>موضوع تماس</h3><p>موضوع تماس‌های ثبت‌شده</p></div></div>${bars(d.callSubject)}</div>\n   <div class=\"panel\" data-source=\"lead\" data-field=\"sourceTicketContactTopic\"><div class=\"panel-head\"><div><h3>موضوع تماس (تیکت منشأ)</h3><p>موضوع تیکت منشأ ثبت‌شده روی لیدها</p></div></div>${bars(d.leadTicketTopic)}</div>\n  </div>\n  <div class=\"panel dimension-detail-panel\" id=\"${period}DimensionDetail\" style=\"display:none\"><div class=\"panel-head\"><div><h3>جزئیات شاخص</h3><p id=\"${period}DimensionDetailSub\">—</p></div><button class=\"clear\" onclick=\"$('${period}DimensionDetail').style.display='none'\">بستن</button></div><div id=\"${period}DimensionDetailBody\"></div></div>\n  <div class=\"panel\"><div class=\"panel-head\"><div><h3>شاخص‌های کیفیت تماس</h3><p>لید یکتا و تماس اضافه روی لید تکراری</p></div></div><div class=\"mini-stats\"><div><b>${fa(k.uniqueCalls)}</b><span>لید یکتای تماس‌شده</span></div><div><b class=\"period-clickable\" data-repeat=\"${period}\">${fa(k.repeatCalls)}</b><span>تماس اضافه روی لید تکراری</span></div><div><b>${k.closeAvg==null?'—':Number(k.closeAvg).toFixed(1)}</b><span>میانگین زمان بستن لید (روز)</span></div><div><b>${fa(s.dayCount)}</b><span>روز موجود در گزارش</span></div></div></div>`;\n\n document.querySelectorAll(`#${pageId} .activity-num`).forEach(el=>el.onclick=()=>loadActivityDetail(el.dataset.p,el.dataset.t,el.dataset.u));\n document.querySelectorAll(`#${pageId} [data-source]`).forEach(panel=>bindBars(panel,v=>loadDimension(period,panel.dataset.source,panel.dataset.field,v)));\n const repeat=document.querySelector(`#${pageId} [data-repeat]`);if(repeat)repeat.onclick=()=>loadRepeated(period);\n if(period!=='daily'){const sel=$(`${period}TrendAdvisor`);if(sel)sel.onchange=()=>loadAdvisorTrend(period,sel.value)}\n bindPeriodFilters(period);\n makeTableSortable($(pageId));\n}\n\nfunction rowsTable(rows){if(!rows||!rows.length)return '<div class=\"empty\">رکوردی وجود ندارد.</div>';const cols=Object.keys(rows[0]);return `<div class=\"table-wrap activity-detail-wrap\"><table class=\"table\"><thead><tr>${cols.map(c=>`<th>${safe(c)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${cols.map(c=>`<td>${safe(r[c]??'—')}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}\nfunction loadActivityDetail(period,type,user){const box=$(`${period}Detail`);box.style.display='block';$(`${period}DetailSub`).textContent='در حال دریافت...';$(`${period}DetailBody`).innerHTML='<div class=\"empty\">در حال دریافت...</div>';google.script.run.withFailureHandler(e=>{$(`${period}DetailBody`).innerHTML='<div class=\"data-note\">'+safe(e.message||e)+'</div>'}).withSuccessHandler(rows=>{$(`${period}DetailSub`).textContent=fa(rows.length)+' رکورد — '+user;$(`${period}DetailBody`).innerHTML=rowsTable(rows);makeTableSortable(box);box.scrollIntoView({behavior:'smooth',block:'start'})}).getActivityDetails(period,type,user)}\nfunction loadDimension(period,source,field,value){const box=$(`${period}DimensionDetail`);box.style.display='block';$(`${period}DimensionDetailSub`).textContent='در حال دریافت...';$(`${period}DimensionDetailBody`).innerHTML='<div class=\"empty\">در حال دریافت...</div>';google.script.run.withFailureHandler(e=>{$(`${period}DimensionDetailBody`).innerHTML='<div class=\"data-note\">'+safe(e.message||e)+'</div>'}).withSuccessHandler(rows=>{$(`${period}DimensionDetailSub`).textContent=`${field}: ${value} — ${fa(rows.length)} رکورد`;$(`${period}DimensionDetailBody`).innerHTML=rowsTable(rows);makeTableSortable(box";
